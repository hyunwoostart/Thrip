<!DOCTYPE html>
<html lang="ko">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>일정 생성</title>
        <script src="https://code.jquery.com/jquery-1.12.4.min.js"></script>
        <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200"/>
        <link rel="stylesheet" href="../public/css/reset.css" />
        <link rel="stylesheet" href="../public/css/common.css" />
        <link rel="stylesheet" href="../public/css/calender.css" />
    </head>
    <body>
        <div id="wrap">
            <!-- 헤더 -->
            <header id="header">
                <nav class="gnb">
                    <ul class="main_menu">
                        <li>
                            <a href="#none" class="ico_back">
                                <span class="blind">뒤로 가기</span>
                            </a>
                        </li>
                        <li>
                            <a href="#none" class="ico_ham">
                                <span class="blind">메뉴</span>
                            </a>
                        </li>
                    </ul>
                </nav>
            </header>
            <!--// 헤더 -->

            <div class="calendarBox">
                <div id="select">
                    <div id="year">
                        <select id="select-year" onchange="changeYear()" aria-placeholder="연도"></select>
                    </div>
                    <div id="month">
                        <select id="select-month" onchange="changeMonth()" aria-placeholder="월"></select>
                    </div>
                </div>
                <div id="departure"></div>
                <div id="arrive"></div>
                <div id="calendar"></div>
                <button type="button" onclick="selectDep()" class="btn" id="selectDep">출발일 선택</button>
                <button type="button" onclick="selectArr()" class="btn hide" id="selectArr">도착일 선택</button>
            </div>
            <div class="scheduleBox">
                <div class="dateBox">
                    <div class="depDate"></div>
                    <span class="material-symbols-outlined"> arrow_right_alt </span>
                    <div class="arrDate"></div>
                </div>
                <button type="button" onclick="selectReset()" id="selectReset">다시 선택</button>
                <form id="schedule-form">
                    <label for="groupName">일정명</label>
                    <input type="text" id="groupName" placeholder="일정명" />
                    <label for="memberSearch">멤버 추가</label>
                    <input type="text" id="memberSearch" placeholder="아이디 추가" />
                    <label for="groupMemo">메모</label>
                    <textarea type="textarea" id="groupMemo" rows="8" placeholder="메모"></textarea>
                    <button type="button" onclick="register()" class="btn">등록</button>
                </form>
            </div>
        </div>
        <script src="../public/js/calender.js"></script>
    </body>
</html>
